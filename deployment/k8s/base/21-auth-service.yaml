apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: novagen
  labels: { app: auth-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: auth-service }
  template:
    metadata:
      labels: { app: auth-service }
    spec:
      containers:
        - name: auth-service
          image: ghcr.io/novagen/novagen-auth-service:latest
          ports: [{ containerPort: 8001 }]
          envFrom:
            - configMapRef: { name: novagen-config }
            - secretRef: { name: novagen-secrets }
          readinessProbe:
            httpGet: { path: /health, port: 8001 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8001 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: novagen
spec:
  selector: { app: auth-service }
  ports: [{ port: 8001, targetPort: 8001 }]
