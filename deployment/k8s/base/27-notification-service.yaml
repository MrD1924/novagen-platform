apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: novagen
  labels: { app: notification-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: notification-service }
  template:
    metadata:
      labels: { app: notification-service }
    spec:
      containers:
        - name: notification-service
          image: ghcr.io/novagen/novagen-notification-service:latest
          ports: [{ containerPort: 8007 }]
          envFrom:
            - configMapRef: { name: novagen-config }
            - secretRef: { name: novagen-secrets }
          readinessProbe:
            httpGet: { path: /health, port: 8007 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8007 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: novagen
spec:
  selector: { app: notification-service }
  ports: [{ port: 8007, targetPort: 8007 }]
