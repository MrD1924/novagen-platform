apiVersion: apps/v1
kind: Deployment
metadata:
  name: prediction-service
  namespace: novagen
  labels: { app: prediction-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: prediction-service }
  template:
    metadata:
      labels: { app: prediction-service }
    spec:
      containers:
        - name: prediction-service
          image: ghcr.io/novagen/novagen-prediction-service:latest
          ports: [{ containerPort: 8003 }]
          envFrom:
            - configMapRef: { name: novagen-config }
            - secretRef: { name: novagen-secrets }
          readinessProbe:
            httpGet: { path: /health, port: 8003 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8003 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: prediction-service
  namespace: novagen
spec:
  selector: { app: prediction-service }
  ports: [{ port: 8003, targetPort: 8003 }]
