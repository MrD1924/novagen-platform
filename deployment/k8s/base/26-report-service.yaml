apiVersion: apps/v1
kind: Deployment
metadata:
  name: report-service
  namespace: novagen
  labels: { app: report-service }
spec:
  replicas: 1
  selector:
    matchLabels: { app: report-service }
  template:
    metadata:
      labels: { app: report-service }
    spec:
      containers:
        - name: report-service
          image: ghcr.io/novagen/novagen-report-service:latest
          ports: [{ containerPort: 8006 }]
          envFrom:
            - configMapRef: { name: novagen-config }
            - secretRef: { name: novagen-secrets }
          readinessProbe:
            httpGet: { path: /health, port: 8006 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8006 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: report-service
  namespace: novagen
spec:
  selector: { app: report-service }
  ports: [{ port: 8006, targetPort: 8006 }]
