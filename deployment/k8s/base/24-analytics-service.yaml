apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: novagen
  labels: { app: analytics-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: analytics-service }
  template:
    metadata:
      labels: { app: analytics-service }
    spec:
      containers:
        - name: analytics-service
          image: ghcr.io/novagen/novagen-analytics-service:latest
          ports: [{ containerPort: 8004 }]
          envFrom:
            - configMapRef: { name: novagen-config }
            - secretRef: { name: novagen-secrets }
          readinessProbe:
            httpGet: { path: /health, port: 8004 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8004 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: novagen
spec:
  selector: { app: analytics-service }
  ports: [{ port: 8004, targetPort: 8004 }]
